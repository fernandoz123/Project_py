# This is a basic workflow to help you get started with Actions
name: Saludo en cada push

on:
  push:

jobs:
  hola:
    runs-on: ubuntu-latest

    steps:
      - name: Saludo personalizado
        run: echo "👋 ¡Hola desde GitHub Actions, soy Fernando!"
